name: skillbox_project
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^0.19.1
  cupertino_icons: ^0.1.3
  carousel_slider:
    git: https://github.com/serenader2014/flutter_carousel_slider

dev_dependencies:
  flutter_test:
    sdk: flutter
  import_sorter: ^4.2.2

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/svg/
